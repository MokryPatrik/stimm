global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8889"]
    scrape_interval: 10s

  # Optional: Add basic metrics from voicebot services if desired
  # - job_name: "voicebot-app"
  #   static_configs:
  #     - targets: ["voicebot-app:8001"]
  #   scrape_interval: 30s